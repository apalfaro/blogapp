apiVersion: v1
kind: Service
metadata:
  name: {{ include "besvc.fullname" . }}
  labels:
    {{- include "besvc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "besvc.selectorLabels" . | nindent 4 }}
